# Hoisting off: a package must not resolve an import it never declared. The
# published tarballs have no runtime dependencies, and this is what keeps that
# true — a phantom import would fail here rather than at a consumer's install.
hoist=false
